#!/bin/bash
set -e
cd "${BASH_SOURCE%/*}"

cd frontend

npm install
npm run build
node djangoBundler.ts &

cd ../backend

pipenv shell
pipenv install --dev

cd ..

./backend/manage.py migrate --noinput
./backend/manage.py collectstatic --noinput --clear
./backend/manage.py runserver "$@"

